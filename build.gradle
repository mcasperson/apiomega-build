buildscript {
    repositories {
        mavenLocal()
        maven {
            url 'https://oss.sonatype.org/content/repositories/snapshots'
        }
    }
    dependencies {
        classpath 'com.apiomega:build:0.0.3-SNAPSHOT'
    }
}

apply plugin: 'com.apiomega.build'
