apply plugin: 'groovy'
apply plugin: 'maven'

group = Group
archivesBaseName = ArchivesBaseName
version = Version

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

